# Platform Access Foundation — план работ

> Статус: **Phase 1 shipped** · Phase 2 partial · Обновлено: 2026-06-02  
> Credentials MVP — [`docs/archive/todos/2.todo-Credentials.archived.md`](./docs/archive/todos/2.todo-Credentials.archived.md)

Канон: `docs/NBOS/02-Modules/07-My-Company/09-Platform-Access-Foundation.md`

---

## Решение

Platform Access Foundation — общая модель для Credentials, Drive, Finance, Project Hub, Tasks и будущих scoped-модулей.

**Phase 1 (schema + resolver + Settings + Project/Product teams + Credentials enforcement):** shipped.

---

## Слайс A — Canon / audit ✅

- [x] Canon `09-Platform-Access-Foundation.md`
- [x] Сверка legacy RBAC docs + vocabulary
- [x] Prisma enums + `@nbos/shared` constants

---

## Слайс B — Project / Product team model ✅

- [x] `ProjectTeamMember` + `ProductTeamMember` (migration `20260602120000_platform_access_foundation`)
- [x] Resolver rules (product team ≠ project access)
- [x] Bridge from legacy Product FK fields + `ProductTeamSyncService`

---

## Слайс C — Role / employee access levels ✅

- [x] `RoleAccessPolicy` + `EmployeeAccessOverride` (schema)
- [x] UI `/settings/access-policies` (Role + Personal tabs, affected users preview)
- [x] Audit / change reason для risky policy edits

---

## Слайс D — Manual resource override

- [x] Schema `ResourceAccessGrant` + enforcement в Credentials
- [x] Credentials: manual levels `View` / `Edit` only
- [x] Credentials Sheet: только вручную добавленные; inherited — summary
- [x] Delete/Admin не assignable как manual level
- [x] Audit grant/update/revoke (Credentials API + audit log tab)
- [x] **Drive runtime:** `drive_folder` + `drive_file_asset` grants + dual-write file grants
- [x] **Reuse contract (docs):** Slice D для Drive/Finance в каноне + `RESOURCE_GRANT_RESOURCE_TYPES` в shared

---

## Слайс E — Credentials integration contract ✅

- [x] Vault scopes, CEO bypass, Team/Project/Secret, delete step-up

---

## Слайс E — Project Hub participants ✅

- [x] `ProjectParticipantsSection` / `ProductParticipantsSection`

---

## Слайс F — Future module reuse

### Drive

- [x] Shared `platform-team-graph.where` + inherited links (PROJECT, PRODUCT, TASK, finance entities)
- [x] File grants dual-write + combined enforcement
- [x] Folder grants + folder→file inherited access + grant count UI
- [x] Listing UI: cards (не chips), tiles white + fluid grid как Credentials
- [x] **Settings `DRIVE` policies → effective `driveScope`** (`drive-effective-scope.ts`, `DriveAccessContextService`, все Drive API endpoints)
- [x] **Polish:** канон foundation (folder grant + inherit + resolver); тесты inherit + merge scope

### Finance

- [x] Invoices, payments, subscriptions, expenses — RBAC VIEW + team graph
- [x] Seller-only boundaries (deal-scoped participation for role `seller`)
- [x] Payroll-only expense paths review (Pay Now skips project participation; `assertExpenseAccessible` on detail/mutations)

### Tasks

- [x] `involvesEmployeeId` / stats + project team graph
- [x] `projectId` list gated by `assertProjectTasksAccessible`
- [x] Workspace-only lists (`workspaceId` gate + participation when no `projectId`)
- [x] Task detail + mutations: `assertTaskAccessible`

### Drive resolver backlog (cleanup register)

- [x] Multi-link confidentiality: tiered inherited resolver (general / finance / legal link families)

---

## Следующий PR (сейчас)

1. **Tasks:** `findByEntity` / checklist item paths — optional access hardening.
2. **Drive:** full Share/Move/Copy permission matrix (cleanup register §3).

---

## Implementation note

Credentials Vault не ждёт foundation. Дальше — Drive Settings resolver (п.2), затем Finance seller boundaries и Slice D docs.
