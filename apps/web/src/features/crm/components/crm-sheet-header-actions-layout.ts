/** Mobile: Call on its own top row (right). Desktop: one inline row. */
export const CRM_SHEET_HEADER_ACTIONS_STACK_CLASS =
  'flex w-full min-w-0 flex-col items-end gap-1.5 md:w-auto md:flex-row md:flex-wrap md:justify-end';

/** Secondary controls under Call on mobile (Связать / Actions / settings). */
export const CRM_SHEET_HEADER_SECONDARY_ACTIONS_CLASS =
  'flex max-w-full min-w-0 flex-wrap items-center justify-end gap-1.5';
