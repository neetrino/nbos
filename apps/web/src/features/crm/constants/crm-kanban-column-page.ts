/** @deprecated Prefer `KANBAN_COLUMN_PAGE_SIZE` from shared kanban. */
export { KANBAN_COLUMN_PAGE_SIZE as CRM_KANBAN_COLUMN_PAGE_SIZE } from '@/features/shared/kanban/kanban-column-page';

/** Trash list fetch size (list-only view; not stage-column paginated). */
export const CRM_TRASH_LIST_PAGE_SIZE = 200;
