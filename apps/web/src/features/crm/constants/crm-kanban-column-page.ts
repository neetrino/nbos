/** Initial cards per CRM kanban column; further rows load on column scroll. */
export const CRM_KANBAN_COLUMN_PAGE_SIZE = 7;

/** Trash list fetch size (list-only view; not stage-column paginated). */
export const CRM_TRASH_LIST_PAGE_SIZE = 200;
