export const COMMERCIAL_DEAL_TYPES = new Set(['PRODUCT', 'EXTENSION', 'OUTSOURCE']);

/** Deal stages where the Handoff readiness panel is shown (before Won). */
export const HANDOFF_VISIBLE_DEAL_STATUSES = new Set(['GET_ANSWER', 'DEPOSIT_AND_CONTRACT', 'WON']);
