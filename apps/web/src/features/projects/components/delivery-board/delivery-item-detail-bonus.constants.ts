/** Bonus entries per page on Delivery Card Bonus tab / drawer (order-scoped list). */
export const DELIVERY_ORDER_BONUS_PAGE_SIZE = 20;

/** Bonus releases per page when expanding an entry in the delivery bonus panel. */
export const DELIVERY_BONUS_RELEASE_PAGE_SIZE = 12;
