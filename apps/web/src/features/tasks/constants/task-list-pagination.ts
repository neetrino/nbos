/** Default fetch size for task boards and workspace runtime lists. */
export const TASK_LIST_PAGE_SIZE = 100;

/** Global `/tasks` list initial fetch (matches API max). */
export const TASK_LIST_GLOBAL_PAGE_SIZE = 200;
