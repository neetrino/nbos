/** Right sheet width for Partner detail — room for Overview → Analytics tabs. */
export const PARTNER_SHEET_CONTENT_WIDTH_CLASS =
  'flex w-full flex-col gap-0 overflow-hidden p-0 data-[side=right]:w-full sm:max-w-none sm:data-[side=right]:w-[62vw]';

export const PARTNER_SHEET_RAIL_ANCHOR_CLASS = 'sm:right-[62vw]';

/** Tab strip: wrap from the left so Analytics stays reachable without a right-side scroll gap. */
export const PARTNER_SHEET_TAB_BAR_SCROLL_CLASS = 'flex-wrap justify-start overflow-x-visible';
