/** Right sheet width for Partner detail — tighter now that tabs are trailing-aligned. */
export const PARTNER_SHEET_CONTENT_WIDTH_CLASS =
  'flex w-full flex-col gap-0 overflow-hidden p-0 data-[side=right]:w-full sm:max-w-none sm:data-[side=right]:w-[58vw]';

export const PARTNER_SHEET_RAIL_ANCHOR_CLASS = 'sm:right-[58vw]';

/** Single-row tab strip, trailing-aligned (Analytics flush at the end). */
export const PARTNER_SHEET_TAB_BAR_SCROLL_CLASS =
  'flex w-full flex-nowrap items-center justify-end gap-1 overflow-x-auto overscroll-x-contain [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden';
