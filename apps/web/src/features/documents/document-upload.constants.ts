/** Max size for inline images uploaded from the document editor. */
export const DOCUMENT_INLINE_IMAGE_MAX_BYTES = 10 * 1024 * 1024;

/** Max size for generic document attachments (non-inline). */
export const DOCUMENT_ATTACHMENT_MAX_BYTES = 25 * 1024 * 1024;
