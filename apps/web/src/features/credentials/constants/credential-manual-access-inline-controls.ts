/** Access level select in manual-access grant rows (default select chrome, no chevron). */
export const CREDENTIAL_MANUAL_ACCESS_ACCESS_SELECT_CLASS =
  'w-auto min-w-[3.75rem] shrink-0 uppercase tracking-wide [&_svg]:hidden';
