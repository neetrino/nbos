'use client';

export function ClientAiPlaceholder() {
  return (
    <p className="px-4 py-1 text-[11px] text-black/40">
      AI draft is a future operator policy. Generating a draft does not send and does not grant
      SEND.
    </p>
  );
}
