/** Shared person contact row chrome (Projects About Contacts + directory cards). */
export const PERSON_CONTACT_ROW_CLASS =
  'border-border bg-card flex w-full min-w-0 items-center gap-3 rounded-xl border px-3.5 py-2';

/** Soft pastel initials — used on Projects Contacts rows (size-10). */
export const PERSON_CONTACT_AVATAR_CLASS =
  'flex size-10 shrink-0 items-center justify-center rounded-full text-xs font-semibold';

/** Compact soft initials — relation picker / task sheet chips (size-8). */
export const PERSON_PICKER_AVATAR_CLASS =
  'flex size-8 shrink-0 items-center justify-center rounded-full text-xs font-semibold';

export const PERSON_CONTACT_OPEN_BTN_CLASS =
  'outline-none focus-visible:ring-2 focus-visible:ring-sky-500/40 focus-visible:ring-offset-1';

export const PERSON_CONTACT_META_STACK_CLASS =
  'flex shrink-0 flex-col items-center justify-center gap-0.5 self-center';
