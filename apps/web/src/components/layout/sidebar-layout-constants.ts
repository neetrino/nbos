/** Expanded sidebar width — must match `Sidebar` root `w-[260px]`. */
export const SIDEBAR_WIDTH_EXPANDED_PX = 260;

/** Collapsed rail width — must match `Sidebar` root `w-[68px]`. */
export const SIDEBAR_WIDTH_COLLAPSED_PX = 68;
