/** Left module name — vertically centered in the header, separated from zone tabs. */
export const HEADER_MODULE_TITLE_WRAP = 'flex shrink-0 items-center gap-2 sm:gap-3';

export const HEADER_MODULE_TITLE_LABEL =
  'nbos-display text-foreground min-w-0 shrink text-[1.35rem] font-semibold tracking-tight whitespace-nowrap max-md:max-w-[11rem] max-md:truncate max-md:font-sans max-md:text-lg max-md:tracking-tight xl:text-[1.65rem]';

/** Vertical rule between module name and zone navigation. Hidden on mobile app chrome. */
export const HEADER_MODULE_TITLE_DIVIDER = 'bg-border h-8 w-px shrink-0 opacity-80 max-md:hidden';

/** Status pill beside the module title — centered in the header row. */
export const HEADER_CONTEXT_STATUS_BADGE_CLASS =
  'self-center shrink-0 rounded-full px-2.5 py-0.5 text-[11px] font-medium';

/** Mobile-only module glyph to the left of the title. */
export const HEADER_MODULE_TITLE_ICON_WRAP = 'md:hidden';