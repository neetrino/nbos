/** Default modal/sheet overlay and popup layer. */
export const OVERLAY_Z_CLASS = 'z-50';

/** Nested dialog/sheet above parent sheet rail (60) and parent overlay (50). */
export const NESTED_OVERLAY_Z_CLASS = 'z-[70]';

/** Portaled popovers, selects, and menus — above nested overlays so pickers stay usable inside sheets/dialogs. */
export const PORTAL_DROPDOWN_Z_CLASS = 'z-[80]';
