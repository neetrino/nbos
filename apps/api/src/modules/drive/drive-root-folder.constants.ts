/**
 * Internal name for the per-space root bucket folder (parentId null).
 * Hidden from folder listings and the folder tree API; holds files at “drive root”.
 */
export const DRIVE_ROOT_STORAGE_FOLDER_NAME = '__nbos_drive_root__';
