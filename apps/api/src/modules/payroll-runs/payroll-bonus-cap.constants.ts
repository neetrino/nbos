/** NBOS default max variable bonus per month (200% of base) when policy has no override. */
export const BONUS_PAYROLL_CAP_BASE_SALARY_MULTIPLIER = 2;

/** Allowed range on `KpiPolicy.bonusCapBaseSalaryMultiplier` (100%–300% of base). */
export const BONUS_PAYROLL_CAP_MULTIPLIER_MIN = 1;

export const BONUS_PAYROLL_CAP_MULTIPLIER_MAX = 3;
