/** Default page size for Work Space list (divisible by 4 for card grid rows). */
export const WORK_SPACES_LIST_DEFAULT_PAGE_SIZE = 24;

export const WORK_SPACES_LIST_MAX_PAGE_SIZE = 100;

export const WORK_SPACES_LIST_MIN_PAGE_SIZE = 1;
