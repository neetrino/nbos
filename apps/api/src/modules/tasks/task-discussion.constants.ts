/** Default page size for task discussion reads. */
export const TASK_DISCUSSION_DEFAULT_PAGE_SIZE = 20;

/** Upper bound so discussion history cannot dump an unbounded thread. */
export const TASK_DISCUSSION_MAX_PAGE_SIZE = 50;

export const TASK_DISCUSSION_MIN_PAGE_SIZE = 1;

/** Maximum stored body length for one discussion entry. */
export const TASK_DISCUSSION_BODY_MAX_LENGTH = 8_000;
