/** Max UTF-8 length for MVP channel/DM body text. */
export const MESSENGER_MESSAGE_BODY_MAX_LENGTH = 10_000;

/** Maximum Drive FileAsset references accepted on a single messenger send. */
export const MESSENGER_MESSAGE_ATTACHMENT_MAX_COUNT = 10;

/** Max length for employee id in DM routes (UUID / cuid). */
export const MESSENGER_USER_ID_PARAM_MAX_LENGTH = 64;
