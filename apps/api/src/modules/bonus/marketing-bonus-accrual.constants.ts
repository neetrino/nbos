/**
 * Planned marketing bonus rates (major units). Zero = preview shows counts only until
 * product configures rates (future: policy template parameters in DB).
 */
export const MARKETING_BONUS_AMOUNT_PER_SQL = 0;

export const MARKETING_BONUS_AMOUNT_PER_MQL = 0;
