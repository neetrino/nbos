/**
 * Planned marketing bonus rates (major units, AMD). Tune here until policy template params ship.
 * Zero disables apply (preview counts only).
 */
export const MARKETING_BONUS_AMOUNT_PER_SQL = 50_000;

export const MARKETING_BONUS_AMOUNT_PER_MQL = 15_000;
