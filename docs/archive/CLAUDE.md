# CLAUDE.md — Archived

This file was moved to archive because it duplicated active AI rules.

Active sources of truth:

- `.cursor/rules/*.mdc`
- User-level rules configured in the workspace

If historical details from the old `docs/CLAUDE.md` are needed,
use git history for commit `b7dcbfd` and earlier.
