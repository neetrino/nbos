# Credentials Vault — план работ (архив)

> **Архив:** 2026-06-02 · MVP закрыт. Активный индекс: [`/todo.md`](../../../todo.md)

> Модуль: `12-Credentials` · Статус: **MVP complete** (vault + security canon)

### Инженерные лимиты (≤300 строк / файл, ≤50 / функция)

| Область                         | Статус                                     |
| ------------------------------- | ------------------------------------------ |
| `credentials/page.tsx`          | ✅ ~167 — hook + main view + overlays      |
| `credential-vault-table` + row  | ✅ ~118 + ~259                             |
| `credentials.service.ts`        | ✅ ~114 facade + operations/utils modules  |
| `credentials.service.*.test.ts` | ✅ list / secrets / mutations + fixture    |
| `use-credential-form-sheet`     | ✅ hooks/ state + actions                  |
| `credential-vault-table-row`    | ✅ row + cells + actions (~57 + ~100 each) |

Канон: `docs/NBOS/02-Modules/12-Credentials/*` · UX decisions: `06-Credentials-UX-Decisions.md`

---

## Зафиксированные решения (2026-06-01)

| #   | Тема                   | Решение                                                                                      | Статус |
| --- | ---------------------- | -------------------------------------------------------------------------------------------- | ------ |
| 1   | ENV bundle             | Paste `.env` → preview → encrypted store → export/copy `.env`                                | ✅     |
| 2   | Форма                  | Dynamic fields по kind/type; не показывать все поля сразу                                    | ✅     |
| 3   | Category vs Type       | **Accepted C-hybrid** — `category` for grouping/slots, `credentialType` for secret format    | ✅     |
| 4   | Criticality / rotation | Auto при create; 4 уровня; override в **Settings** внутри Sheet                              | ✅     |
| 5   | Comment                | Одно поле, **always private** (encrypted); в Sheet **виден сразу** при доступе (без step-up) | ✅     |
| —   | Create/Edit UI         | **Sheet** вместо modal dialog; удалить dialog после реализации                               | ✅     |
| —   | Vault UI shell         | `My` / `Team` / `Project` / `Secret` / `Archived` + 3 view modes + unified Sheet             | ✅     |
| —   | Access foundation      | Platform model: role levels + personal overrides + manual credential override                | ✅     |

---

## Слайс 0 — Rich seed ✅

- [x] `seed-credentials-demo.ts` (~100 записей, encrypted secrets, all access levels)
- [x] Подключено в `seed.ts`, seed прогнан

---

## Слайс A — UX ✅

### Form / Sheet

- [x] Глобальный аудит Category vs Type → decision record
- [x] `CredentialFormSheet` (create + edit), удалить modal dialogs
- [x] Dynamic secret fields по kind/type
- [x] ENV paste + preview (export/copy via step-up в Sheet)
- [x] Comment → single encrypted field; убрать public notes из UI
- [x] Settings block: criticality override, next rotation override (auto defaults on create)
- [x] Backend: auto criticality + default `nextRotationAt` on create

### List / vault

- [x] Scope tabs: `All`, `My`, `Team`, `Project`, `Secret` (+ Archived toggle)
- [x] Hide create button in `All` and `Archived`; create only from scope tabs
- [x] View switch: `List`, `Tiles`, `Category Board`
- [x] `Category Board`: columns by scope category + `+` presets category in create Sheet
- [x] Sort Recently used / Name / Newest in filter panel
- [x] Quick filters + API filters
- [x] Step-up dialog (reveal/copy)
- [x] Project CredentialsTab → deep-link to Sheet
- [x] Pagination List/Tiles + Category Board infinite scroll

---

## Слайс B — Операционный vault ✅

- [x] Manual Access tab in Sheet (VIEW/EDIT, expiry, employee sheet overlay)
- [x] Audit log tab в Sheet
- [x] Export encrypted file + step-up

---

## Слайс C — Security canon ✅

- [x] Manual grant expiry
- [x] Emergency access
- [x] Secret versions history
- [x] Offboarding hook
- [x] Permanent delete step-up HIGH/CRITICAL

---

## Слайс D — Scale (MVP) ✅

- [x] Bulk archive / restore / export (max 50)

### Отложено

- [~] CSV import
- [~] Saved views / favorites (platform-wide)
- [~] Bitrix encrypted migration

---

## Backlog (не блокирует MVP)

- [ ] Расширенные фильтры: Project, Product, Client, Provider, period
- [ ] UX: Recently used без полного Reset
- [ ] Bulk: assign owner, tags, rotation request, move category

---

## Ссылки

- UX: `docs/NBOS/02-Modules/12-Credentials/06-Credentials-UX-Decisions.md`
- Cleanup: `docs/NBOS/02-Modules/12-Credentials/99-Credentials-Cleanup-Register.md`
- Tests: `apps/api/src/modules/credentials/credentials.service.*.test.ts`
- Access foundation (активный Phase 2): [`1.todo-Access.md`](../../../1.todo-Access.md)
