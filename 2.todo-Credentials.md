# Credentials Vault — план работ

> Модуль: `12-Credentials` · Статус: MVP + rich seed · Обновлено: 2026-06-02

Канон: `docs/NBOS/02-Modules/12-Credentials/*` · UX decisions: `06-Credentials-UX-Decisions.md`

---

## Зафиксированные решения (2026-06-01)

| #   | Тема                   | Решение                                                                                      | Статус |
| --- | ---------------------- | -------------------------------------------------------------------------------------------- | ------ |
| 1   | ENV bundle             | Paste `.env` → preview → encrypted store → export/copy `.env`                                | ✅     |
| 2   | Форма                  | Dynamic fields по kind/type; не показывать все поля сразу                                    | ✅     |
| 3   | Category vs Type       | **Accepted C-hybrid** — `category` for grouping/slots, `credentialType` for secret format    | ✅     |
| 4   | Criticality / rotation | Auto при create; 4 уровня; override в **Settings** внутри Sheet                              | ✅     |
| 5   | Comment                | Одно поле, **always private** (encrypted); в Sheet **виден сразу** при доступе (без step-up) | ✅     |
| —   | Create/Edit UI         | **Sheet** вместо modal dialog; удалить dialog после реализации                               | ✅     |
| —   | Vault UI shell         | `My` / `Team` / `Project` / `Secret` / `Archived` + 3 view modes + unified Sheet             | ✅     |
| —   | Access foundation      | Platform model: role levels + personal overrides + manual credential override                | ✅     |

**Слайс A разблокирован:** п.3 закрыт → можно делать Sheet + dynamic fields.

---

## Слайс 0 — Rich seed ✅

- [x] `seed-credentials-demo.ts` (~100 записей, encrypted secrets, all access levels)
- [x] Подключено в `seed.ts`, seed прогнан

---

## Слайс A — UX

### Form / Sheet

- [x] Глобальный аудит Category vs Type → decision record
- [ ] `CredentialFormSheet` (create + edit), удалить modal dialogs
- [ ] Dynamic secret fields по kind/type
- [ ] ENV paste + preview + export `.env`
- [ ] Comment → single encrypted field; убрать public notes из UI
- [ ] Settings block: criticality override, next rotation override (auto defaults on create)
- [ ] Backend: auto criticality + default `nextRotationAt` on create (если ещё нет)

### List / vault

- [ ] Scope tabs: `All`, `My`, `Team`, `Project`, `Secret`, `Archived`
- [ ] Hide create button in `All` and `Archived`; create only from `My` / `Team` / `Project` / `Secret`
- [ ] View switch: `List`, `Tiles`, `Category Board`
- [ ] `Category Board`: columns by scope category + `+` on each column presets category in create Sheet
- [ ] Compact `Recently/Frequently used` strip for `List` and `Tiles` only; hidden in `Category Board`
- [ ] Quick filters under search: Hosting, Domain, Mail, Admin, API, Database, Mine, Needs Rotation
- [ ] Фильтры API + UI: category, credentialType, rotationStatus, productId, ownerId
- [ ] **Needs Rotation** as quick filter / saved view, not scope tab
- [ ] Step-up dialog вместо `window.prompt` (для password/api/env reveal)
- [ ] Project CredentialsTab → «Open in Vault» / deep-link to Sheet
- [ ] Pagination (убрать hardcoded `pageSize: 200`)

---

## Слайс B — Операционный vault

- [ ] Manual Access block in Sheet: only manually added employees; per-row `View` / `Edit` / `Remove`
- [ ] Audit log block в Sheet
- [ ] Recently used
- [ ] Export → encrypted file download

---

## Dependency — Platform Access Foundation

- [x] Phase 1 foundation shipped ([1.todo-Access.md](./1.todo-Access.md))
- [x] API visibility uses `ProjectTeamMember` / `ProductTeamMember` + `ResourceAccessGrant` (legacy `allowedEmployees` + delivery FK fallback retained)
- [ ] Vault scope tabs UI (`My` / `Team` / `Project` / `Secret` / `Archived`) — blocked on form only if needed; list tabs can follow
- [ ] Sheet Manual Access block — depends on unified Sheet implementation

---

## Слайс C — Security canon

- [ ] CredentialAccessGrant + expiry
- [ ] Emergency access
- [ ] Versions on rotation
- [ ] Offboarding hook
- [ ] Permanent delete step-up для CRITICAL

---

## Слайс D — Scale & migration

- [ ] Bulk actions
- [ ] CSV import
- [ ] Saved views / favorites
- [ ] Bitrix encrypted migration

---

## Решение — п.3 Category / Type

**Accepted:** C-hybrid.

DB сохраняет оба поля:

- `category` = grouping, Category Board columns, quick filters, Delivery slots, Finance/product handoff grouping.
- `credentialType` = secret format, dynamic fields, ENV editor, reveal/copy/export behavior and validation.

**UI rule:** не показывать два параллельных dropdown `Category` + `Credential Type` как sibling controls. Scope выбран через tab; category задается/preset-ится context/board/slot/compact control; credentialType выбирается как `What is stored?`.

**Enum rule:** не добавлять `ENV`, `SSH`, `RECOVERY_CODES` в category; это credentialType formats.

**Затронутые места для реализации:**

- Prisma enums `CredentialCategoryEnum`, `CredentialTypeEnum`
- `product-access-slots.ts`, Delivery Board bind/create
- `product-done-readiness.ts` handoff categories
- List filters, seed, migration mapping

**Можно начинать** form/sheet по rules из `06-Credentials-UX-Decisions.md`.

---

## Решение — access model / уровни доступа

**Статус:** direction accepted; implementation requires Platform Access Foundation above.

**Accepted concepts:**

- Platform access foundation: role levels + personal employee overrides + manual per-credential override.
- `ProjectTeamMember` and `ProductTeamMember` are separate concepts.
- Product team does not automatically get project-level access to all products.
- Project team/admin level is higher and can manage project/participants/settings by policy.
- Manual override in credential Sheet lists only manually added employees and supports `View` / `Edit`.
- Delete remains high-level RBAC permission, not a manual credential level.

---

## Прочие решения (ранее)

| Вопрос                                 | Вариант                                                       |
| -------------------------------------- | ------------------------------------------------------------- |
| Access model на P1                     | Superseded by Platform Access Foundation (`1.todo-Access.md`) |
| Step-up для secrets                    | Password-only (не Comment)                                    |
| DEVELOPMENT/MARKETING/FINANCE category | Отложить                                                      |

---

## Ссылки

- UX decisions: `docs/NBOS/02-Modules/12-Credentials/06-Credentials-UX-Decisions.md`
- Cleanup: `99-Credentials-Cleanup-Register.md`
- Regression: `apps/api/src/modules/credentials/credentials.service.test.ts`
